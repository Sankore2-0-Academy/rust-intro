/**
 * Primitive Types:--
 * Integers: u8, i8, u16, i16, u32, i32, u64, i64, u128, i128, (number of bits they take in memory)
 * Floats: f32, f64
 * Boolean (bool)
 * Characters (char)
 * Tuples
 * Arrays
 */

pub fn run() {
 let age: u32 = 65;
 let money: f64 = 2000.50;
 let character = '\u{1F600}';

 println!("{}", character);
}